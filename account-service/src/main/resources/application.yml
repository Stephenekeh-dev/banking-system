server:
  port: 8082   # account-service runs on port 8082

spring:
  datasource:
    url: jdbc:postgresql://account_postgres:5432/accountdb
    username: postgres
    password: 1234567Steven..,
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # auto create tables (change to validate in prod)
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  application:
    name: account-service
